{
  "name": "troy",
  "private": true,
  "version": "1.0.0",
  "description": "troy: wallpaper marketing site for MacBook and iPhone.",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client",
    "vercel-build": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && NPM_CONFIG_PRODUCTION=false npm install --prefix server && npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
